# Grasped Cargo project anatomy quickly

User completed Lesson 1 (`cargo new`, `Cargo.toml`/`src/main.rs` roles, `cargo run`, `println!` formatting) and reported it as easy — pure structural/ecosystem understanding, no friction. Despite starting "from scratch" per stated preference, mechanical/setup-type concepts (tooling, project layout) don't need heavy hand-holding; pace can move a bit faster here. Save the slow, deliberate pacing for concepts with real Rust-specific difficulty (ownership, borrowing, lifetimes) rather than tooling.
